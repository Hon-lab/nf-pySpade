# Branch Setup Instructions

## dev_mn Branch Created

A new development branch named `dev_mn` has been successfully created for optimization work on the nf-pySpade pipeline.

### Current Status
- ✅ Branch `dev_mn` created locally
- ✅ Branch contains all latest changes from the base branch
- ✅ Branch documentation added in `.branch-info.md`
- ⚠️ Branch needs to be pushed to remote repository

### Manual Step Required
To push the `dev_mn` branch to the remote repository, run:

```bash
git push -u origin dev_mn
```

This will create the branch on GitHub and set up tracking between the local and remote branch.

### Branch Purpose
The `dev_mn` branch is dedicated to implementing and testing optimization improvements to the nf-pySpade Nextflow pipeline. Developers can use this branch to:
- Test performance optimizations
- Implement code improvements
- Experiment with new features
- Conduct benchmarking

### Next Steps
1. Push the branch to remote: `git push -u origin dev_mn`
2. Start making optimization changes
3. Create pull requests from `dev_mn` when optimizations are ready for review
